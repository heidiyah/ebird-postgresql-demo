-For observation_count (set to text) will need to cast to numeric when querying numerically.
Ex:
SELECT value::NUMERIC
FROM example
WHERE value != 'X';

-sed has more recent contents than ebd, will need to consider when joining

-To run postgres commands server-side:
    1. ssh root@192.168.0.53 #run terminal inside server
    2. docker exec -it ix-postgres-postgres-1 /bin/bash
    3. cd to desired directory
        - ~/data/heidi/backups  #backups
        - ~/data/heidi/ebd_sampling_relJul-2025/ebd_sampling_relJul-2025    # ebd data
        - 
    4. run commands
